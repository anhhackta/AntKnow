using UnityEngine;

/*public enum TileType { Start, Property, Tax, Bonus, Chance, Jail, GoToJail, FreeParking }
public enum Owner { None = 0, P1 = 1, P2 = 2, P3 = 3, P4 = 4 }

public sealed class PlayerState {
  public int Id;                // 1..N
  public int Money;
  public int NodeIndex;         // ô hiện tại (0..BoardLength-1)
  public int JailTurns;         // 0 nếu tự do
  public List<int> OwnedPropertyIds = new();
}

public sealed class PropertyState {
  public int TileId;
  public Owner Owner = Owner.None;
  public int HouseCount;        // 0..4 (+1 biệt thự nếu bạn dùng)
  public int BasePrice;
}

public sealed class GameState {
  public int CurrentTurnPlayerId;      // lượt ai
  public int Round;                     // số lượt toàn bàn
  public List<PlayerState> Players = new();
  public Dictionary<int, PropertyState> Properties = new(); // key = TileId
  public int BoardLength;
}*/
